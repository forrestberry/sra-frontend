/**
 * Placeholder Supabase database definition.
 * Replace with generated types (`supabase gen types typescript --local`)
 * once the backend schema is available locally.
 */
export type Database = Record<string, never>;
